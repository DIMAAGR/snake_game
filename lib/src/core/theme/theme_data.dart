import 'package:flutter/material.dart';

class ApplicationTheme {
  ThemeData get whiteTheme => ThemeData(backgroundColor: const Color.fromARGB(255, 250, 250, 250));
  ThemeData get darkTheme => ThemeData(backgroundColor: const Color.fromARGB(255, 40, 40, 40));
}
